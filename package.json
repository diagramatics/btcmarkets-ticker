{
  "name": "btcmarkets-ticker",
  "version": "0.1.0",
  "description": "A PWA for BTCMarkets market data.",
  "author": "Steven Sinatra <ohai@diagramatics.me>",
  "license": "MIT",
  "dependencies": {
    "lerna": "^2.5.1"
  },
  "scripts": {
    "start": "lerna run start --stream",
    "deploy": "lerna exec --concurrency 1 -- npm run deploy && now alias crypto.dgr.li -r rules.json"
  }
}
